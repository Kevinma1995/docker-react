sudo: required
services: 
    - docker

before_install:
    - docker build -t zhaochunm/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true zhaochunm/docker-react npm run test -- --coverage
